{
  "name": "@tim-smart/torch",
  "version": "1.0.1",
  "description": "Library to turn on/off the camera flash on phones",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf dist/ .cache/",
    "build": "npm run clean && tsc",
    "prepare": "npm run build",
    "browser-test": "npm run clean && parcel serve test/index.html",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "webcam",
    "torch"
  ],
  "author": "Tim Smart <tim@fostle.com>",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.10.3",
    "prettier": "^1.15.3",
    "tslint": "^5.11.0",
    "tslint-config-prettier": "^1.17.0",
    "typescript": "^3.2.2"
  }
}
